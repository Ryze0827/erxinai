# Local Personal Context

This file is for machine-specific or person-specific notes that help local execution in this repository.

Keep it local-only:
- Do not commit it.
- Do not promote its contents directly into `AGENTS.md`, `CLAUDE.md`, `project-profile.md`, `engineering-constraints.md`, or shared recall.
- Re-verify and sanitize any fact before turning it into repository-wide guidance.

## Local Paths

- Companion workspace local paths:
- Export or download directories:
- Local caches, SDKs, or tool installs:

## Local Tooling And Environment

- Proxy or mirror settings:
- Package manager or registry overrides:
- Local service endpoints:
- Secrets or credential source:

## Personal Working Notes

- Frequently used commands:
- Preferred debug entry points:
- Temporary reminders safe to keep local:
